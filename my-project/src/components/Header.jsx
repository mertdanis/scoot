import { NavLink, Link } from "react-router-dom";

function Header() {
  return (
    <nav>
      <ul>
        <NavLink>About</NavLink>
        <NavLink>About</NavLink>
        <NavLink>About</NavLink>
      </ul>
    </nav>
  );
}

export default Header;
